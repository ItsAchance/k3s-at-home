apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: linkding-claim0
  name: linkding-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
